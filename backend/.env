# ====== Common ======
APP_NAME="PAAA Backend"
SECRET_KEY="1234"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=60

# ====== Full stack (Docker) ======
DATABASE_URL_USERS=postgresql://postgres:postgres@database_users:5432/paadb_users
DATABASE_URL_DATA=postgresql://postgres:postgres@database_etl:5432/paadb_data

MINIO_ENDPOINT=http://minio:9000
MINIO_ACCESS_KEY=minio
MINIO_SECRET_KEY=minio123
MINIO_BUCKET=paaa

# ====== Isolated backend (no external services) ======
# Use SQLite files locally; comment-out the Postgres URLs above and uncomment these:
# DATABASE_URL_USERS=sqlite:///./data/users.db
# DATABASE_URL_DATA=sqlite:///./data/data.db
# For local storage fallback, leave MINIO_* unset to use ./storage folder
